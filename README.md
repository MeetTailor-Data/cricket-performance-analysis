# cricket-performance-analysis
The Cricket Performance Analysis project is a Python application that uses the NumPy library to analyze players' runs scored across multiple matches. The program performs multi-dimensional array operations to calculate total and average runs per player, match-wise statistics, identify best and worst performers, detect consistent players.
